import Foundation

enum EyeEntity {
    struct Configuration {
        // Add parameters here later if needed (e.g., type, animationSpeed, etc.)
    }

    /// Default configuration
    static let `default` = Configuration()
}
